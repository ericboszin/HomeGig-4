<p>
  <strong>Description:</strong>
  <%= bid.description %>
</p>
 
<p>
  <strong>Amount:</strong>
  <%= bid.amount %>
</p>

<% if current_user == User.find(bid.user_id) %>
  <%= link_to 'Delete Bid', [bid.job, bid],
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %>
<% end %>
